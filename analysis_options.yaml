include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    # strict-casts and strict-inference intentionally OFF:
    # - strict-casts surfaces ~96 errors at JSON boundaries (json.decode →
    #   typed call sites) that are contract-enforced at the caller.
    # - strict-inference flags ~190 places where Dart can't infer generic
    #   type args (Map(), Future(), [], etc.) — mostly cosmetic noise from
    #   a codebase that predates strict-mode.
    # strict-raw-types stays on: it catches the actually-dangerous `List`,
    # `Map`, `Future` declarations without type args.
    strict-raw-types: true
  errors:
    invalid_annotation_target: ignore
    todo: ignore
  exclude:
    - lib/l10n/intl/**
    - lib/**/*.g.dart
    - lib/**/*.freezed.dart
    - lib/models/generated/**
    - build/**
    - plugins/**

linter:
  rules:
    # Style — single source of truth on quoting + trailing commas
    prefer_single_quotes: true
    require_trailing_commas: true

    # Safety — null and async correctness
    avoid_returning_null_for_future: true
    cancel_subscriptions: true
    close_sinks: true
    no_adjacent_strings_in_list: true
    no_duplicate_case_values: true
    throw_in_finally: true
    unawaited_futures: true

    # Quality — readability and intent
    always_declare_return_types: true
    avoid_empty_else: true
    avoid_print: true
    avoid_relative_lib_imports: true
    avoid_returning_this: true
    avoid_unused_constructor_parameters: true
    join_return_with_assignment: true
    no_runtimeType_toString: true
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_final_in_for_each: true
    prefer_final_locals: true
    prefer_interpolation_to_compose_strings: true
    prefer_typing_uninitialized_variables: true
    sort_pub_dependencies: true
    test_types_in_equals: true
    unnecessary_parenthesis: true
    unnecessary_statements: true
    use_super_parameters: true
