// Network Rules v1: data model placeholder.
//
// Concrete types (Rule, Condition, Action, NetworkSnapshot) land in Wave 1.
// Wave 0 keeps this file empty on purpose so the failing engine_test
// in test/network_rules/ stays the single tripwire for the bootstrap.
