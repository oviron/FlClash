org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1g -XX:+UseG1GC -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true
org.gradle.daemon=true
android.useAndroidX=true
# Verified 2026-05-18: no com.android.support:* deps in any configuration
# (`./gradlew :app:dependencies | grep "com.android.support"` returns empty
# for both classic + bydpi runtime classpaths). Jetifier rewriting is unused;
# turning it off skips dependency-resolution rewriting and saves warm-build time.
android.enableJetifier=false
android.nonTransitiveRClass=true
kotlin.incremental=true
kotlin.daemon.jvmargs=-Xmx2G -XX:+UseG1GC
kotlin.code.style=official
</content>
</invoke>